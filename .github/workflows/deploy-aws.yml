name: deployment-disabled-use-governed-local-dispatcher

on:
  workflow_dispatch:

jobs:
  deployment-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "CyberAudit deployment is exclusively authorized through scripts/deploy-aws-local.sh"
